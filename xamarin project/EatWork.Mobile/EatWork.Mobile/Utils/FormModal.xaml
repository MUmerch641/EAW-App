<?xml version="1.0" encoding="utf-8" ?>
<popup:PopupPage xmlns="http://xamarin.com/schemas/2014/forms"
                 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                 xmlns:popup="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
                 xmlns:animate="clr-namespace:Rg.Plugins.Popup.Animations;assembly=Rg.Plugins.Popup"
                 x:Class="EatWork.Mobile.Utils.FormModal"
                 x:Name="formModalBasePage">
    <popup:PopupPage.Resources>
        <!--<ResourceDictionary>
             <ResourceDictionary.MergedDictionaries>
             <ResourceDictionary Source="Styles.xaml" />
             </ResourceDictionary.MergedDictionaries>
             </ResourceDictionary>-->
    </popup:PopupPage.Resources>

    <popup:PopupPage.Animation>
        <animate:ScaleAnimation DurationIn="400"
                                DurationOut="300"
                                EasingIn="SinOut"
                                EasingOut="SinIn"
                                HasBackgroundAnimation="True"
                                PositionIn="Right"
                                PositionOut="Right"
                                ScaleIn="1.2"
                                ScaleOut="0.8" />
    </popup:PopupPage.Animation>

    <ContentPage.Content>
        <Grid Padding="0" Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="{OnIdiom Desktop=1*, Tablet=1*, Phone=20}" />
                <ColumnDefinition Width="{OnIdiom Desktop=3*, Tablet=4*, Phone=*}" />
                <ColumnDefinition Width="{OnIdiom Desktop=1*, Tablet=1*, Phone=20}" />
            </Grid.ColumnDefinitions>

            <Frame BackgroundColor="{DynamicResource Gray-White}"
                   BorderColor="{StaticResource Gray-White}"
                   HorizontalOptions="Center"
                   VerticalOptions="Center"
                   Padding="0"
                   CornerRadius="8"
                   Grid.Column="1">

                <ScrollView HorizontalScrollBarVisibility="Never" VerticalScrollBarVisibility="Never">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="45" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Grid BackgroundColor="{DynamicResource Gray-White}">
                            <Grid.RowDefinitions>
                                <!--<RowDefinition Height="*" />-->
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <ContentView Content="{Binding Source={x:Reference formModalBasePage},Path=Header}" />
                        </Grid>

                        <Grid RowSpacing="0" Grid.Row="1" Padding="20,10,20,30" BackgroundColor="{StaticResource Gray-White}">

                            <Grid.RowDefinitions>
                                <!--<RowDefinition Height="*" />-->
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <ContentView Content="{Binding Source={x:Reference formModalBasePage}, Path=Body}"
                                         HorizontalOptions="Center" />
                        </Grid>
                    </Grid>
                </ScrollView>
            </Frame>
        </Grid>
    </ContentPage.Content>
</popup:PopupPage>