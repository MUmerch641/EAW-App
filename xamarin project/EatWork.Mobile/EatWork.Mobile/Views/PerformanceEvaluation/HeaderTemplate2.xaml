<?xml version="1.0" encoding="utf-8" ?>
<StackLayout xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:avatar="clr-namespace:Syncfusion.XForms.AvatarView;assembly=Syncfusion.Core.XForms"
             xmlns:progressBar="clr-namespace:Syncfusion.XForms.ProgressBar;assembly=Syncfusion.SfProgressBar.XForms"
             x:Class="EatWork.Mobile.Views.PerformanceEvaluation.HeaderTemplate2">
    <StackLayout.Resources>
        <ResourceDictionary>
            <Style x:Key="TitleLabelStyle" TargetType="Label">
                <Setter Property="FontSize" Value="16" />
                <Setter Property="TextColor" Value="{DynamicResource Gray-900}" />
                <Setter Property="HorizontalOptions" Value="Center" />
                <Setter Property="HorizontalTextAlignment" Value="Center" />
                <Setter Property="FontFamily" Value="{DynamicResource SemiBold}" />
                <Setter Property="LineHeight" Value="{OnPlatform Android=1.5, Default=-1}" />
            </Style>

            <Style x:Key="DescriptionLabelStyle" TargetType="Label">
                <Setter Property="FontSize" Value="14" />
                <Setter Property="TextColor" Value="{DynamicResource Gray-700}" />
                <Setter Property="HorizontalOptions" Value="Start" />
                <Setter Property="HorizontalTextAlignment" Value="Start" />
                <Setter Property="FontFamily" Value="{DynamicResource Medium}" />
                <Setter Property="LineHeight" Value="{OnPlatform Android=1.5, Default=-1}" />
            </Style>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Requests/Styles.xaml" />
                <ResourceDictionary Source="../Template/Styles.xaml" />
                <ResourceDictionary Source="../Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </StackLayout.Resources>

    <!--HEADER-->
    <Grid ColumnDefinitions="Auto, *, Auto"
          Margin="0,0">

        <avatar:SfAvatarView Style="{DynamicResource AvatarProfileImageStyle}"
                             ContentType="AvatarCharacter"
                             HeightRequest="80"
                             WidthRequest="80"
                             CornerRadius="55"
                             Margin="8,0,8,8"
                             Grid.Column="0" />

        <Label Grid.Column="1"
               Style="{StaticResource LabelStyleMySchedule}"
               Margin="8,0,0,4"
               VerticalOptions="Center"
               HorizontalOptions="Center"
               TextColor="{DynamicResource Gray-700}"
               Text="{Binding Holder.Period}" />

        <progressBar:SfCircularProgressBar WidthRequest="95"
                                           HeightRequest="95"
                                           Progress="{Binding Holder.Progress}"
                                           Margin="8,0,0,8"
                                           Minimum="0"
                                           Maximum="100"
                                           VerticalOptions="CenterAndExpand"
                                           TrackColor="#3351483a"
                                           ProgressColor="{StaticResource Success}"
                                           Grid.Column="2" 
                                           ShowProgressValue="False">
            <progressBar:SfCircularProgressBar.Content>
                <Grid RowDefinitions="Auto">
                    <Label TextColor="{StaticResource Success}" 
                           Text="{Binding Holder.Progress,StringFormat='{0}%'}" 
                           HorizontalTextAlignment="Center" 
                           VerticalTextAlignment="End"/>
                </Grid>
            </progressBar:SfCircularProgressBar.Content>
        </progressBar:SfCircularProgressBar>
    </Grid>
    
    <!--SEPARATOR-->
    <BoxView Style="{DynamicResource SeparatorStyle}" Grid.Row="2" BackgroundColor="{DynamicResource Gray-300}" Margin="8,5" />
</StackLayout>