<?xml version="1.0" encoding="utf-8" ?>
<popup:PopupPage xmlns="http://xamarin.com/schemas/2014/forms"
                 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:d="http://xamarin.com/schemas/2014/forms/design"
                 xmlns:utils="clr-namespace:EatWork.Mobile.Utils"
                 xmlns:iconize="clr-namespace:Plugin.Iconize;assembly=Plugin.Iconize"
                 xmlns:animate="clr-namespace:Rg.Plugins.Popup.Animations;assembly=Rg.Plugins.Popup"
                 xmlns:popup="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
                 xmlns:gradient="clr-namespace:Syncfusion.XForms.Graphics;assembly=Syncfusion.Core.XForms"
                 mc:Ignorable="d"
                 x:Class="EatWork.Mobile.Views.Dialogs.DialogBase"
                 x:Name="dialogBasePage">

    <popup:PopupPage.Animation>
        <animate:ScaleAnimation DurationIn="400"
                                DurationOut="300"
                                EasingIn="SinOut"
                                EasingOut="SinIn"
                                HasBackgroundAnimation="True"
                                PositionIn="Center"
                                PositionOut="Right"
                                ScaleIn="1.2"
                                ScaleOut="0.8" />
    </popup:PopupPage.Animation>

    <ContentPage.Content>
        <Grid Padding="0" Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="{OnIdiom Desktop=1*, Tablet=1*, Phone=20}" />
                <ColumnDefinition Width="{OnIdiom Desktop=3*, Tablet=4*, Phone=*}" />
                <ColumnDefinition Width="{OnIdiom Desktop=1*, Tablet=1*, Phone=20}" />
            </Grid.ColumnDefinitions>

            <utils:CustomShadowFrame BackgroundColor="{StaticResource Gray-White}"
                                     BorderColor="{StaticResource Gray-White}"
                                     HorizontalOptions="Center"
                                     VerticalOptions="Center"
                                     Padding="0"
                                     CornerRadius="0"
                                     Grid.Column="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="45" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid BackgroundColor="{DynamicResource PrimaryColor}">

                        <gradient:SfGradientView>
                            <gradient:SfGradientView.BackgroundBrush>
                                <gradient:SfLinearGradientBrush StartPoint="0, 0.5" EndPoint="1, 0.5">
                                    <gradient:SfLinearGradientBrush.GradientStops>
                                        <gradient:SfGradientStop Color="{DynamicResource PrimaryColor}" Offset="0.0" />
                                        <gradient:SfGradientStop Color="{StaticResource PrimaryGradient}" Offset="1.0" />
                                    </gradient:SfLinearGradientBrush.GradientStops>
                                </gradient:SfLinearGradientBrush>
                            </gradient:SfGradientView.BackgroundBrush>
                        </gradient:SfGradientView>

                        <Grid ColumnSpacing="0">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <iconize:IconLabel Grid.Column="0"
                                               Style="{StaticResource IconizeIconStyle}"
                                               Text="{StaticResource CommentAltIcon}"
                                               TextColor="{StaticResource Gray-White}"
                                               Padding="15,10,0,10"
                                               FontSize="Large" />

                            <Label Text="Message"
                                   TextColor="{StaticResource Gray-White}"
                                   FontSize="16"
                                   Padding="0,10"
                                   Grid.Column="1"
                                   FontFamily="{DynamicResource Medium}"
                                   x:Name="lblHeaderText" />
                        </Grid>
                    </Grid>

                    <Grid RowSpacing="0" Grid.Row="1" Padding="20,10,20,30" BackgroundColor="{StaticResource Gray-White}">

                        <Grid.RowDefinitions>
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Label x:Name="lblMessage"
                               Grid.Row="0"
                               VerticalTextAlignment="Start"
                               HorizontalTextAlignment="Start"
                               HorizontalOptions="Start"
                               VerticalOptions="StartAndExpand"
                               FontSize="14"
                               FontFamily="{DynamicResource Medium}"
                               TextColor="{StaticResource Gray-800}" />

                        <ContentView Grid.Row="1"
                                     Content="{Binding Source={x:Reference dialogBasePage}, Path=ActionsPlaceHolder}"
                                     HorizontalOptions="Center"
                                     VerticalOptions="End" />
                    </Grid>
                </Grid>
            </utils:CustomShadowFrame>
        </Grid>
    </ContentPage.Content>
</popup:PopupPage>