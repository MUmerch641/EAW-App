<?xml version="1.0" encoding="utf-8" ?>
<StackLayout xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sflistview="clr-namespace:Syncfusion.ListView.XForms;assembly=Syncfusion.SfListView.XForms"
             xmlns:buttons="clr-namespace:Syncfusion.XForms.Buttons;assembly=Syncfusion.Buttons.XForms"
             xmlns:utils="clr-namespace:EatWork.Mobile.Utils"
			 xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
             ios:Page.UseSafeArea="true"
             mc:Ignorable="d"
             x:Class="EatWork.Mobile.Views.Shared.SelectableListView">

    <StackLayout.Resources>
        <ResourceDictionary>
            <utils:CustomConverter x:Key="EventArgs" />
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </StackLayout.Resources>

    <sflistview:SfListView x:Name="SelectableNameList"
                           Style="{StaticResource SfListViewStyle}"
                           AutoFitMode="Height"
                           ItemSpacing="0"
                           SelectionGesture="Tap"
                           SelectionMode="Single"
                           IsScrollBarVisible="True"
                           ItemsSource="{Binding SelectableListItemSource}">
        <sflistview:SfListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <ViewCell.View>
                        <Grid RowSpacing="0" ColumnSpacing="0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <buttons:SfCheckBox Grid.Row="0"
                                                HeightRequest="48"
                                                Text="{Binding DisplayText}"
                                                Margin="16,0"
                                                HorizontalOptions="Start"
                                                VerticalOptions="Center"
                                                TextColor="{DynamicResource Gray-900}"
                                                IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                                CornerRadius="2"
                                                FontFamily="{DynamicResource Medium}"
                                                UncheckedColor="{DynamicResource Gray-300}"
                                                FontSize="{StaticResource Small}">

                                <buttons:SfCheckBox.Behaviors>
                                    <utils:SfCheckBoxCommandBehavior EventName="StateChanged"
                                                                     Command="{Binding Path=BindingContext.SelectedTransactionTypeCommand, Source={x:Reference SelectableNameList}}"
                                                                     Converter="{StaticResource EventArgs}"
                                                                     CommandParameter="{Binding .}" />
                                </buttons:SfCheckBox.Behaviors>
                            </buttons:SfCheckBox>

                            <BoxView Grid.Row="1" Style="{StaticResource SeparatorStyle}" />
                        </Grid>
                    </ViewCell.View>
                </ViewCell>
            </DataTemplate>
        </sflistview:SfListView.ItemTemplate>
    </sflistview:SfListView>
</StackLayout>