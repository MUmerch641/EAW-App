<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:ffsvg="clr-namespace:FFImageLoading.Svg.Forms;assembly=FFImageLoading.Svg.Forms"
             BackgroundColor="{StaticResource Gray-White}"
             xmlns:core="clr-namespace:Syncfusion.XForms.Core;assembly=Syncfusion.Core.XForms"
             x:Class="EatWork.Mobile.Views.Shared.NoInternetConnectionPage">

    <ContentPage.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>
        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackLayout BackgroundColor="Transparent"
                         Spacing="0"
                         VerticalOptions="Center"
                         Padding="20,0,20,0">

                <!--  Image for display svg image  -->
                <ffsvg:SvgCachedImage x:Name="ErrorImage"
                                      Margin="{x:OnIdiom Phone='0,0,0,0', Desktop='0',Tablet='0'}"
                                      BackgroundColor="Transparent"
                                      HeightRequest="{core:OnPlatformOrientationDoubleValue Default=300,
                                      AndroidPhonePortrait=200,
                                      AndroidPhoneLandscape=140,
                                      iOSPhonePortrait=200,
                                      iOSPhoneLandscape=140}"
                                      Source="NoInternet.svg"
                                      VerticalOptions="Center"
                                      WidthRequest="{x:OnIdiom Phone=200, Desktop=300, Tablet=300}"
                                      FadeAnimationEnabled="False" />

                <!--  Label to display header  -->
                <Label Margin="0,32,0,8"
                       FontSize="20"
                       FontFamily="{DynamicResource SemiBold}"
                       HorizontalTextAlignment="Center"
                       Text="NO INTERNET"
                       Style="{StaticResource LabelStyle}"
                       VerticalTextAlignment="Center"
                       x:Name="lblContent" />

                <!--  Label to display content  -->
                <Label Margin="8,0,8,0"
                       FontSize="16"
                       Text="You must be connected to the internet to complete this action"
                       Style="{StaticResource LabelStyle}"
                       FontFamily="{DynamicResource Medium}"
                       VerticalTextAlignment="Center"
                       x:Name="lblMessage" />
            </StackLayout>

            <!--  Try again button  -->
            <!--<button:SfButton Grid.Row="1" Text="TRY AGAIN" />-->
        </Grid>
    </ContentPage.Content>
</ContentPage>