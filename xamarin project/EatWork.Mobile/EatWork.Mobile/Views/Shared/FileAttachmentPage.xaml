<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:iconize="clr-namespace:Plugin.Iconize;assembly=Plugin.Iconize"
             xmlns:buttons="clr-namespace:Syncfusion.XForms.Buttons;assembly=Syncfusion.Buttons.XForms"
             xmlns:syncfusion="clr-namespace:Syncfusion.ListView.XForms;assembly=Syncfusion.SfListView.XForms"
             xmlns:converter="clr-namespace:EatWork.Mobile.Utils"
             xmlns:shared="clr-namespace:EatWork.Mobile.Views.Shared"
             xmlns:busyindicator="clr-namespace:Syncfusion.SfBusyIndicator.XForms;assembly=Syncfusion.SfBusyIndicator.XForms"
             xmlns:button="clr-namespace:Syncfusion.XForms.Buttons;assembly=Syncfusion.Buttons.XForms"
             mc:Ignorable="d"
             xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
             ios:Page.UseSafeArea="true"
             x:Class="EatWork.Mobile.Views.Shared.FileAttachmentPage">
    <ContentPage.Resources>
        <ResourceDictionary>
            <converter:TextToBoolConverter x:Key="TextToBoolConverter" />
            <Style x:Key="btnStyle" TargetType="buttons:SfButton">
                <Setter Property="CornerRadius" Value="3" />
                <Setter Property="BackgroundColor" Value="White" />
                <Setter Property="BorderColor" Value="Black" />
                <Setter Property="BorderWidth" Value="0.5" />
                <Setter Property="WidthRequest" Value="1000" />
                <Setter Property="HeightRequest" Value="45" />
                <Setter Property="VerticalOptions" Value="EndAndExpand" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <AbsoluteLayout VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
            <busyindicator:SfBusyIndicator x:Name="busyindicator"
                                           Style="{StaticResource BusyIndicatorStyle}"
                                           IsBusy="{Binding IsBusy}" />

            <StackLayout AbsoluteLayout.LayoutFlags="PositionProportional"
                         AbsoluteLayout.LayoutBounds="0.5,0.5,-1,-1"
                         IsVisible="{Binding IsBusy,Converter={converter:InverseBoolConverter}}">

                <shared:NoItemsPartialPage TitleText="NO DOCUMENTS"
                                           DetailsText="No items available to display."
                                           ImageSource="Resume.svg"
                                           IsVisible="{Binding FileAttachmentList, Converter={converter:ListToBoolConverter}}" />
            </StackLayout>

            <Grid RowSpacing="0" ColumnSpacing="0"
                  AbsoluteLayout.LayoutFlags="All"
                  AbsoluteLayout.LayoutBounds="0,0,1,1">
                <Grid.RowDefinitions>
                    <!--<RowDefinition Height="Auto" />
                         <RowDefinition Height="Auto" />-->
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <StackLayout Grid.Row="0" Margin="10,10" Spacing="0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="80" />
                        </Grid.ColumnDefinitions>

                        <Label Text="File Attachments" HorizontalOptions="Start"
                               FontSize="20"
                               FontFamily="{DynamicResource Bold}"
                               TextColor="{DynamicResource Gray-900}"
                               Grid.Column="0" />

                        <button:SfButton x:Name="SkipButton"
                                         Grid.Column="1"
                                         BackgroundColor="Transparent"
                                         Command="{Binding CloseCommand}"
                                         CornerRadius="20"
                                         FontFamily="{DynamicResource SemiBold}"
                                         HeightRequest="40"
                                         HorizontalOptions="End"
                                         Text="Close"
                                         TextColor="{x:StaticResource HyperLink}"
                                         VerticalOptions="StartAndExpand"
                                         WidthRequest="80" />
                    </Grid>

                    <Grid RowSpacing="0" ColumnSpacing="0" Margin="0,20,0,0">

                        <syncfusion:SfListView x:Name="FileAttachmentListView"
                                               ItemsSource="{Binding FileAttachmentList}"
                                               AutoFitMode="Height"
                                               SelectionMode="None"
                                               LoadMoreOption="AutoOnScroll"
                                               TapCommand="{Binding DownloadCommand}"
                                               AllowSwiping="False"
                                               ItemSpacing="0,0,0,1"
                                               IsVisible="{Binding FileAttachmentList,Converter={converter:ListToInverseBoolConverter}}">

                            <!--<syncfusion:SfListView.GroupHeaderTemplate>
                                 <DataTemplate>
                                 <ViewCell>
                                 <ViewCell.View>
                                 <StackLayout Padding="10,7,0,0">
                                 <Label Text="{Binding Key}"
                                 FontSize="18"
                                 TextColor="Gray"
                                 FontAttributes="Bold"
                                 HorizontalOptions="StartAndExpand"
                                 VerticalOptions="Center" />
                                 </StackLayout>
                                 </ViewCell.View>
                                 </ViewCell>
                                 </DataTemplate>
                                 </syncfusion:SfListView.GroupHeaderTemplate>-->

                            <syncfusion:SfListView.RightSwipeTemplate>
                                <DataTemplate x:Name="RightSwipeTemplate">
                                    <Grid>
                                        <Grid BackgroundColor="Transparent" HorizontalOptions="Fill" VerticalOptions="Fill" Grid.Column="0">
                                            <Grid VerticalOptions="Center" HorizontalOptions="Center" ColumnSpacing="14" Padding="15,0">
                                                <iconize:IconImage Grid.Column="0"
                                                                   Grid.Row="0"
                                                                   BackgroundColor="Transparent"
                                                                   IconColor="Red"
                                                                   Icon="fas-trash-alt"
                                                                   HeightRequest="20"
                                                                   WidthRequest="20">
                                                    <iconize:IconImage.GestureRecognizers>
                                                        <TapGestureRecognizer Command="{Binding Path=BindingContext.DeleteFileCommand, Source={x:Reference FileAttachmentListView}}" CommandParameter="{Binding .}" />
                                                    </iconize:IconImage.GestureRecognizers>
                                                </iconize:IconImage>
                                            </Grid>
                                        </Grid>
                                    </Grid>
                                </DataTemplate>
                            </syncfusion:SfListView.RightSwipeTemplate>

                            <syncfusion:SfListView.ItemTemplate>
                                <DataTemplate>
                                    <ViewCell>
                                        <ViewCell.View>
                                            <Grid Padding="2" Margin="1">
                                                <converter:CustomShadowFrame BackgroundColor="{StaticResource Gray-White}"
                                                                             BorderColor="{StaticResource Gray-White}"
                                                                             BorderWidth="1"
                                                                             CornerRadius="8"
                                                                             HasShadow="true"
                                                                             HorizontalOptions="FillAndExpand"
                                                                             Radius="4"
                                                                             ShadowOffSetHeight="4"
                                                                             ShadowOffsetWidth="0"
                                                                             ShadowOpacity="0.12"
                                                                             Margin="1">
                                                    <StackLayout>
                                                        <Grid>
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition Height="*" />
                                                            </Grid.RowDefinitions>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="30" />
                                                                <ColumnDefinition Width="*" />
                                                            </Grid.ColumnDefinitions>
                                                            <StackLayout Grid.Column="0">
                                                                <iconize:IconImage Icon="fas-file-download"
                                                                                   WidthRequest="30"
                                                                                   HeightRequest="20"
                                                                                   IconColor="{DynamicResource PrimaryColor}"
                                                                                   HorizontalOptions="StartAndExpand"
                                                                                   VerticalOptions="CenterAndExpand" >
                                                                    <iconize:IconImage.GestureRecognizers>
                                                                        <TapGestureRecognizer Command="{Binding Path=BindingContext.DownloadCommand, Source={x:Reference FileAttachmentListView}}" CommandParameter="{Binding .}" />
                                                                    </iconize:IconImage.GestureRecognizers>
                                                                </iconize:IconImage>
                                                            </StackLayout>
                                                            <StackLayout Grid.Column="1">
                                                                <Label Text="{Binding FileName}"
                                                                       LineBreakMode="NoWrap"
                                                                       FontSize="{StaticResource Small}"
                                                                       FontFamily="{DynamicResource SemiBold}"
                                                                       HorizontalOptions="StartAndExpand"
                                                                       VerticalOptions="CenterAndExpand"
                                                                       TextColor="{StaticResource Gray-800}" />
                                                            </StackLayout>
                                                        </Grid>
                                                    </StackLayout>

                                                    <!--<Grid Grid.Column="0"
                                                         RowSpacing="1"
                                                         Padding="10,0,10,0"
                                                         VerticalOptions="Start">
                                                         <Grid.RowDefinitions>
                                                         <RowDefinition Height="20" />
                                                         <RowDefinition Height="*" />
                                                         <RowDefinition Height="*" />
                                                         <RowDefinition Height="*" />
                                                         </Grid.RowDefinitions>
                                                         <Grid.ColumnDefinitions>
                                                         <ColumnDefinition Width="*" />
                                                         <ColumnDefinition Width="*" />
                                                         </Grid.ColumnDefinitions>
                                                         </Grid>-->
                                                </converter:CustomShadowFrame>
                                            </Grid>
                                        </ViewCell.View>
                                    </ViewCell>
                                </DataTemplate>
                            </syncfusion:SfListView.ItemTemplate>
                        </syncfusion:SfListView>
                    </Grid>
                </StackLayout>
            </Grid>
        </AbsoluteLayout>
    </ContentPage.Content>
</ContentPage>